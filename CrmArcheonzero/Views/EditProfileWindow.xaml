<Window x:Class="CrmArcheonzero.Views.EditProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование профиля" 
        Height="250" Width="400"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        Background="White">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="✏️ Редактирование профиля" 
                   FontSize="20" FontWeight="Bold" 
                   Foreground="{StaticResource AccentDeepBlue}"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Имя:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="FullNameBox" Padding="10" FontSize="14" 
                     BorderBrush="#DDD" BorderThickness="1"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Email:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailBox" Padding="10" FontSize="14" 
                     BorderBrush="#DDD" BorderThickness="1"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Сохранить" 
                    Click="Save_Click" 
                    Width="120" Height="40" Margin="5"
                    Background="{StaticResource AccentDeepBlue}" 
                    Foreground="White" FontWeight="Bold" FontSize="14"
                    Cursor="Hand"/>
            
            <Button Content="Отмена" 
                    Click="Cancel_Click" 
                    Width="120" Height="40" Margin="5"
                    Background="#F5F5F5" Foreground="#333"
                    FontWeight="Bold" FontSize="14"
                    Cursor="Hand"/>
        </StackPanel>

        <TextBlock Grid.Row="4" 
                   x:Name="StatusText" 
                   Foreground="Red" 
                   HorizontalAlignment="Center" 
                   Margin="0,10,0,0"/>
    </Grid>
</Window>